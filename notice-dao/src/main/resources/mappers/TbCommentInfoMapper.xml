<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.silita.notice.dao.TbCommentInfoMapper" >
    <!-- update mishu.`tb_comment_info` set related_id=#{ntId} where related_id=#{id}-->
    <update id="updateRelatedId" parameterType="map">
        update mishu.`tb_comment_info` set related_id=#{relatedId} where pkid=#{pkid}
    </update>
    <update id="updateRelatedId2" parameterType="map">
        update mishu.`tb_reply_comment` set related_id=#{relatedId} where pkid=#{pkid}
    </update>

    <select id="queryCountComment" resultType="java.lang.Integer" parameterType="map">
    select count(0) from mishu.`tb_comment_info` where related_id=#{id} and source=#{source}
  </select>
    <select id="querySource" resultType="java.lang.String" parameterType="map">
        select source from mishu.`tb_comment_info` group by source
    </select>
    <select id="queryRelatedId" resultType="java.util.Map" parameterType="map">
        SELECT related_id as relatedId,pkid FROM mishu.`tb_comment_info`
    </select>
    <select id="queryRelatedId2" resultType="java.util.Map" parameterType="map">
        SELECT related_id as relatedId,pkid FROM mishu.`tb_reply_comment`
    </select>
</mapper>